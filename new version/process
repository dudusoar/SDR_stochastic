
1 real map: real map, real data map, 导入数据，生成坐标，生成real index
2 demands：demand generator，生成pairs，生成所有需求
3 order info：生成scnarios，包含所有的时间信息、距离矩阵（mile）、时间矩阵（min），用real index作为索引
4 instance：解包order table，生成用order index作为索引的所有数据结构
5.solution
6.operators，solvers
7.sensitivity analysis、